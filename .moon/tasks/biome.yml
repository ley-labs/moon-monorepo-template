$schema: 'https://moonrepo.dev/schemas/tasks.json'

inheritedBy:
  file: 'biome.json'

tasks:
  lint:
    command: 'pnpm biome check --no-errors-on-unmatched --files-ignore-unknown=true .'
    inputs:
      - 'src/**/*'
      - 'biome.json'
      - '/biome.json'
