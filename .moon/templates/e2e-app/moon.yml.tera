# yaml-language-server: $schema=../../.moon/cache/schemas/project.json

language: 'typescript'
stack: 'unknown'
layer: 'automation'
tags: ['e2e']

dependsOn:
  - '{{ projectName }}-web'
  - '{{ projectName }}-api'
