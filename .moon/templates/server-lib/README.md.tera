# @ley-labs/{{ projectName }}-server

Server-only library for authentication and authorization.

## Scripts

- `moon run {{ projectName }}-server:build` - Build for production.
- `moon run {{ projectName }}-server:test` - Run tests.
- `moon run {{ projectName }}-server:lint` - Check for linting errors.
- `moon run {{ projectName }}-server:typecheck` - Run TypeScript type checking.

## Usage

### Install

From the workspace root:
```bash
bun add @ley-labs/{{ projectName }}-server --workspace --filter <target-app>
```

### Import

```typescript
import { serverOnly } from '@ley-labs/{{ projectName }}-server';
```
