# yaml-language-server: $schema=../../../.moon/cache/schemas/template.json

id: 'web-app'
title: 'Web Application Template'
description: 'A SolidJS web application using Vite and Cloudflare Pages.'
destination: '/apps/[projectName]-web'

variables:
  projectName:
    type: 'string'
    prompt: 'What is the name of the project?'
    required: true
