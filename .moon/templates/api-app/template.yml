# yaml-language-server: $schema=../../../.moon/cache/schemas/template.json

id: 'api-app'
title: 'API Application Template'
description: 'A Cloudflare Workers API application using Hono.'
destination: '/apps/[projectName]-api'

variables:
  projectName:
    type: 'string'
    prompt: 'What is the name of the project?'
    required: true
